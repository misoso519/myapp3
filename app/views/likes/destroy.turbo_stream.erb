<turbo-stream target="<%= dom_id(@likeable, :like) %>" action="replace">
  <template>
    <%= render partial: "likes/like_button", formats: :html, locals: { likeable: @likeable } %>
  </template>
</turbo-stream>
